166
# ^ The executed-test FLOOR for `ios/test.command` with no --filter.
#
# The first run of digits is the only thing parsed; everything below is for humans.
#
# WHY IT EXISTS. This gate was killed for reporting success with zero tests executed:
# `./test.command --filter <anything that matches nothing>` printed
# "Test run with 0 tests in 0 suites passed" and exited 0. Requiring a non-zero count
# closes that. This file closes the other half - a suite that quietly stops being
# DISCOVERED (a renamed file, a target dropped from Package.swift, a @Suite that no
# longer compiles in) shrinks the count without failing anything, and the gate stays
# green over a smaller and smaller surface.
#
# HOW TO CHANGE IT. Raise it in the same commit that adds tests. LOWER it only in the
# same commit that removes them, so the reduction appears in the diff and somebody has
# to justify it in a message. Never edit it to turn a red gate green.
#
# 42 = MQEngineJS bridge (10) + MQContent models (8) + Figure spec decoding (14, incl.
#      parameterised cases) + Grading parity corpus (4) + Engine lifetime and memory (6)
#      as of the fix pass on lane/engine-bridge, 2026-09-07.
#
# 71 = those 42 + MQDesign's 29, wired into the root manifest on feat/ios-phase1,
#      2026-09-07:
#        Tokens resolve                       10  (type scale, space, motion, tap floor,
#                                                  3 palettes x roles, times of day, theme)
#        Metrics classify the matrix          14  (12 device sizes + 2 explicit cases)
#        Typesetting: units never break        6
#        Bundled fonts register and resolve    8  (5 weights + register + idempotent +
#                                                  a negative control)
#        Components render without overflow   12  (sign, 2 figures, 2 tiles, chrome,
#                                                  4 creatures, 2 tokens, marker floor)
#        Every screen fits every device       24  (12 sizes x overflow, 12 x tap floor)
#        The cast never crosses the parchment  4  (the 4 landscape sizes)
#      The parameterised suites are counted by CASE here because that is what the
#      runner's summary counts; the 29 is the delta the runner reported (42 -> 71).

# 143 = those 71 + 72 from lane/patchwerk (MQPatchwerk 55 + MQServices 17), 2026-09-07:
#        Patchwerk scoring parity with the web    4  (corpus shape, the mirrored CONFIG,
#                                                     300 recorded runs replayed as 300
#                                                     parameterised cases, and the same
#                                                     300 counted again in one test)
#        The fight is the clock                   8  (a whole tier per case, the enrage
#                                                     boundary per tier, expiry, the stun,
#                                                     monotonic damage, the clock label,
#                                                     the boss refill, the record shape)
#        Stacks, freezes and the anti-quit rules   8
#        The Patchwerk question feed               8  (topic filter, pool weights, harder
#                                                     with stacks, the no-repeat ring,
#                                                     bounded redraws, pool-not-feed draws)
#        The mirrored PRNG                         4
#        Stem shapes                               5
#        A whole fight, picker to board            9
#        Patchwerk screens fit the glass           7  (fit and tap floor across 4 devices x
#                                                     5 screens, the overlay contract, the
#                                                     hourglass floor across the 12-size
#                                                     matrix, the trap's negative control,
#                                                     and the PNG render)
#        The device leaderboard                    9
#        Leaderboard names                         6
#        Game Center is a stub, and admits it      2
#      NOTE the runner counts a parameterised @Test as ONE here, not once per case - the
#      300-run parity replay is one line in this total, not 300. The MQDesign entry above
#      counted cases because that is what the runner reported at the time; if the two ever
#      have to be reconciled, the floor is whatever an unfiltered run prints, nothing more.

# 166 = those 143 + 23 from the FIX PASS on lane/patchwerk answering the 2026-09-07
#      refutation (SURVIVED-wounded, 7 wounds):
#        Patchwerk scoring parity with the web    +1  (W1: the corpus's recorded
#                                                      sourceSha256 against a sha-256
#                                                      this suite computes from
#                                                      js/modes/patchwerk.js on disk.
#                                                      The node half of the same pin is
#                                                      `node tools/make-patchwerk-parity.mjs
#                                                      --check`, now inside `npm test`.)
#        The Patchwerk HUD, measured on the pixels +16 (W2: 12 device sizes x 3 scenes x
#                                                      5 controls, drawn frame vs overlay
#                                                      frame out of two separate renders,
#                                                      counted as 12 parameterised cases;
#                                                      the probe's negative control; W3:
#                                                      the sand at 0:21, the sand at four
#                                                      moments of a fight, and the wire
#                                                      from PatchwerkRun.sandFraction)
#        The clock cannot be driven backwards,    +7  (W6: monotonicity over 200,000 reads,
#        and the ticker is one per run                 real-time advance, a backwards clock
#                                                      on all 3 tiers, the clamp at every
#                                                      boundary, and the web-parity argument
#                                                      as an assertion. W7: 100 view
#                                                      re-inits make no timer, 100 starts
#                                                      make one, and stop() really stops.)
#        Leaderboard names agree with the web     +4  (W4: the corpus pinned to
#                                                      api/leaderboard.js by sha, all 7,053
#                                                      names, coverage of the decomposition
#                                                      class, and the named cases)
#        An unreadable board is kept, never       +5  (W5: quarantine, the schema case,
#        clobbered                                     writes refused when the move aside
#                                                      fails, a healthy board untouched,
#                                                      and a missing file is not a
#                                                      corruption)
#      Minus nothing: no test was removed. The old `overlayMatchesTheDrawnScreen` was the
#      tautology the refutation named; it is kept, renamed to what it actually checks
#      (the wiring), with the real gate now beside it in HUDPixelTests.
