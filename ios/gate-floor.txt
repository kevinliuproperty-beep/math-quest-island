443
# ^ The executed-test FLOOR for `ios/test.command` with no --filter.
#
# The first run of digits is the only thing test.command parses; the `key: value` lines
# at the bottom are parsed by the SUITES (see GateFloor.swift, one copy in each test
# target). Everything else is for humans.
#
# WHY IT EXISTS. This gate was killed for reporting success with zero tests executed:
# `./test.command --filter <anything that matches nothing>` printed
# "Test run with 0 tests in 0 suites passed" and exited 0. Requiring a non-zero count
# closes that. This file closes the other half - a suite that quietly stops being
# DISCOVERED (a renamed file, a target dropped from Package.swift, a @Suite that no
# longer compiles in) shrinks the count without failing anything, and the gate stays
# green over a smaller and smaller surface.
#
# HOW TO CHANGE IT. Raise it in the same commit that adds tests. LOWER it only in the
# same commit that removes them, so the reduction appears in the diff and somebody has
# to justify it in a message. Never edit it to turn a red gate green.
#
# ===========================================================================
# 443 = the PHASE 1 REHEARSAL FIX PASS, 2026-09-07. The clean-room dress rehearsal
#       ruled the branch NOT READY on four blockers; 41 tests were added closing
#       them, and every one of the four is now behind a gate that fails when the
#       defect is put back. Measured off `--filter <target>`, and they sum to 443:
#
#   MQContentTests         21   unchanged
#   MQEngineJSTests        21   unchanged
#   MQDesignTests          43   +12: the answer tile's measured advance and line
#                               height, three-line layout, plank words, and the
#                               4.5:1 caption/subtitle contrast floor with its
#                               grounds enumerated
#   MQProgressTests        97   unchanged
#   MQQuestTests          114   +23: PROGRESS SURVIVES THE PROCESS (12 items on a
#                               FILE store, reopened cold: skills, sessions,
#                               crystals, node state), profile isolation, the `+`
#                               token end to end, "no source file builds a
#                               ProfileID out of a display string", the Patchwerk
#                               plank and the new-explorer sheet at 12 sizes, the
#                               level badge under a long name, the pie's asked
#                               category over 500 real pies x 3 slots, the longest
#                               option of all 250 refs x 12 sizes, the L-shape's
#                               closure, the line graph's y axis, figure growth
#   MQPatchwerkTests       74   +6: a driven 2-minute run in which every item is
#                               answerable and every carried figure is drawn, the
#                               typed arena's fit and tap targets at 12 sizes
#   MQServicesTests        27   unchanged
#   MQCubeContentTests     10   unchanged
#   MQCubeEngineJSTests    36   unchanged
#   ------------------------------------------------------------------------
#                         443
#
# NEGATIVE CONTROLS RUN, not just written. Putting `ProfileID(chosen.profile.name)`
# back takes 6 of the 8 persistence tests red with 18 issues, every one naming the
# rehearsal's own symptom (skills 0, sessions 0, crystals 0 against 6, node .open).
# Restoring `.fixedSize()` on the hero token's name takes the badge probe from 117
# sampled pixels to 0. Restoring the pre-fix L-shape draw order fails the closure
# gate at all three slots.
#
# ---------------------------------------------------------------------------
# 402 = the PHASE 1 INTEGRATION, 2026-09-07: feat/ios-phase1 with lane/quest (which
#       already carried feat/unit-sweep), lane/progress, lane/patchwerk and
#       lane/cube-extract merged, plus the integrator's own reconciliation.
#
# MEASURED, NOT ADDED UP. Each lane raised this file independently against its own
# branch, so the four lanes' numbers (160, 167, 166, 88) share the 42 + 29 that were
# already here and cannot be summed. The floor is what `./test.command` actually
# executed on the merged tree, and the per-target counts below were each read off
# `./test.command --filter <target>`. They sum to exactly 402, which is the check that
# nothing is being counted twice or not at all.
#
#   MQContentTests         21   the seam: models, Verdict.Reason, the eight-kind Figure
#   MQEngineJSTests        21   the JavaScriptCore bridge, lifetime, grading parity
#   MQDesignTests          31   tokens, metrics, typesetting, fonts, the device matrix,
#                               + 2 NEW: a screen leaves no holes in its own background
#   MQProgressTests        97   mastery, the one-way fade, persistence, migration,
#                               profiles, the web-crystal corpus, the Patchwerk fence,
#                               + 1 NEW: 40,000 adversarial fade events on the FILE
#                               backend, across 20 reopens
#   MQQuestTests           91   the flow, typed entry, unit chips, figures, the pixel
#                               gate, the driver, + 2 NEW: the crystal end to end
#   MQPatchwerkTests       68   scoring parity, the HUD in pixels, the clock, the ticker
#   MQServicesTests        27   the on-device board, the name filter, the GC stub
#   MQCubeContentTests     10   cube state, domain refusals
#   MQCubeEngineJSTests    36   the cube bridge, the golden corpus, words, parity, solves
#   ------------------------------------------------------------------------
#                         402
#
# Counted by CASE where a @Test is parameterised, because that is what the runner's
# summary line counts and the summary is what test.command parses.
#
# WHAT THE INTEGRATION ADDED (5, on top of the 397 the merge itself produced):
#   MQDesignTests    "No screen leaves a transparent pixel on the 9.7 inch iPad" (2 cases)
#                    and its negative control - MQWorld left an unpainted wedge along the
#                    shoreline, which is what "the dark line right of the crab" actually
#                    was: 261 px of near-zero alpha at y=784 on the 9.7" landscape battle.
#   MQQuestTests     "QRunState fells a monster exactly where the web awarded a crystal"
#                    and "Reported through Attempt.crystalsReported, every session lands
#                    on the web's rope" - 200 real web sessions, battle to store.
#   MQProgressTests  "40,000 adversarial events on the FILE backend, across reopens,
#                    never rise" - the lane proved this in a session scratchpad that died
#                    with the session, and the load path is the half W10 was actually
#                    about. A proof nobody can re-run is a story about a proof.
#
# WHAT THE INTEGRATION DID NOT LOSE. Three MQQuestTests were REWRITTEN rather than
# removed when the lanes' stand-in stores were deleted: they were asserting the deleted
# stand-in's exponential mastery curve, which the real store does not have. Same three
# tests, same names, now graded against MQProgressStore's actual rule. The count did not
# drop, and this note is here so a future reader does not have to reconstruct why.
#
# ---------------------------------------------------------------------------
# THE LANES' OWN LINEAGE, kept because each number is a claim somebody proved
# ---------------------------------------------------------------------------
#
# 42 = MQEngineJS bridge (10) + MQContent models (8) + Figure spec decoding (14, incl.
#      parameterised cases) + Grading parity corpus (4) + Engine lifetime and memory (6)
#      as of the fix pass on lane/engine-bridge, 2026-09-07.
#
# 71 = those 42 + MQDesign's 29, wired into the root manifest on feat/ios-phase1.
#
# 143 = those 71 + MQQuest's 72, added on lane/quest.
# 160 = those 143 + MQQuest's 17 from the lane/quest FIX PASS (Quest Refutation K1-K7).
#
# 136 = those 71 + MQProgress's 65, added on lane/progress.
# 167 = those 136 + 31 from the lane/progress FIX PASS (the twelve Progress Refutation
#       wounds: crystals, the Cleared policies, the review figures, privacy on disk).
#
# 143 -> 166 on lane/patchwerk: MQPatchwerk + MQServices, then the fix pass's 23 for the
#       seven Patchwerk Refutation wounds (the pinned corpus, the pause knob's real rect,
#       the hourglass sand, the unicode name filter, the corrupt board, the monotonic
#       clock, the single ticker).
#
# 80 = the 42 above + the cube engine, added by lane/cube-extract off main.
# 88 = the 80 above + the cube fix pass (the CUBE_API golden corpus, the state-domain
#      refusals, the snapshot's word surface), answering the extract refutation's two
#      kills.
#
# Each of those was measured on its own branch. On the merged tree the shared 71 is
# counted once, which is why 402 and not 160+167+166+88.
#
# ---------------------------------------------------------------------------
# SAMPLE-SIZE FLOORS  (wound 2 of the extract refutation)
# ---------------------------------------------------------------------------
# `MQ_CUBE_PARITY_ROWS=1 MQ_CUBE_SOLVES=1 MQ_DRAWS=1 ios/test.command` used to print
# "80 tests, floor cleared, GATE PASSED" in 1.3 s instead of 5.4 s: every suite asserted
# `checked == rows.count`, which is true for ANY cap. The test count was floored and the
# amount each test measured was not.
#
# So the sizes are floored here too, and the suites read these keys. On a gate run
# (test.command exports MQ_GATE=1 when no --filter and no --skip is given) setting the
# matching environment variable is a FAILURE naming the variable, not a silently honoured
# cap.
#
# Raise these when the corpora grow. Lower them only in the commit that shrinks the
# corpus, for the same reason as the test count.

cube-parity-rows: 10000
# ^ MQ_CUBE_PARITY_ROWS. Rows replayed PER SIZE out of tools/fixtures/cube-parity-corpus.json
#   (20,000 across both sizes), in node by tools/cube-parity-test.mjs and in JavaScriptCore
#   by CubeParityCorpusTests.

cube-solves: 12
# ^ MQ_CUBE_SOLVES. Full 50-move scrambles solved beat by beat through the bridge, per size.

cube-golden-states: 300
# ^ States per size replayed out of tools/fixtures/cube-api-golden.json - the value oracle
#   for CUBE_API, which is not one of the four guarded blocks and which eleven refuter
#   corruptions rode straight through every gate before that file existed. The corpus
#   carries 320; the floor is 300 so a trim is visible without being brittle.

cube-golden-asks: 300
# ^ bestQuestion calls per size in CubeGoldenTests, which replays every FOURTH golden row
#   (one ask is a completion search plus two full ask rankings, ~27 ms through
#   JavaScriptCore, so the whole battery is 70 s of a 5 s gate). The exhaustive walk over
#   all 640 paintings runs on the node side in cube-engine-sanity section G; this floor
#   stops the Swift stride quietly becoming "the first row".

engine-draws: 200
# ^ MQ_DRAWS. Draws per generator ref in the question engine's bridge gate (250 refs).
