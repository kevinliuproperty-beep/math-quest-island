160
# ^ The executed-test FLOOR for `ios/test.command` with no --filter.
#
# The first run of digits is the only thing parsed; everything below is for humans.
#
# WHY IT EXISTS. This gate was killed for reporting success with zero tests executed:
# `./test.command --filter <anything that matches nothing>` printed
# "Test run with 0 tests in 0 suites passed" and exited 0. Requiring a non-zero count
# closes that. This file closes the other half - a suite that quietly stops being
# DISCOVERED (a renamed file, a target dropped from Package.swift, a @Suite that no
# longer compiles in) shrinks the count without failing anything, and the gate stays
# green over a smaller and smaller surface.
#
# HOW TO CHANGE IT. Raise it in the same commit that adds tests. LOWER it only in the
# same commit that removes them, so the reduction appears in the diff and somebody has
# to justify it in a message. Never edit it to turn a red gate green.
#
# 42 = MQEngineJS bridge (10) + MQContent models (8) + Figure spec decoding (14, incl.
#      parameterised cases) + Grading parity corpus (4) + Engine lifetime and memory (6)
#      as of the fix pass on lane/engine-bridge, 2026-09-07.
#
# 71 = those 42 + MQDesign's 29, wired into the root manifest on feat/ios-phase1,
#      2026-09-07:
#        Tokens resolve                       10  (type scale, space, motion, tap floor,
#                                                  3 palettes x roles, times of day, theme)
#        Metrics classify the matrix          14  (12 device sizes + 2 explicit cases)
#        Typesetting: units never break        6
#        Bundled fonts register and resolve    8  (5 weights + register + idempotent +
#                                                  a negative control)
#        Components render without overflow   12  (sign, 2 figures, 2 tiles, chrome,
#                                                  4 creatures, 2 tokens, marker floor)
#        Every screen fits every device       24  (12 sizes x overflow, 12 x tap floor)
#        The cast never crosses the parchment  4  (the 4 landscape sizes)
#      The parameterised suites are counted by CASE here because that is what the
#      runner's summary counts; the 29 is the delta the runner reported (42 -> 71).
#
# 143 = those 71 + MQQuest's 72, added on lane/quest, 2026-09-07:
#         HP and streak follow the web's own formulas   14  (6 damage cases +
#                                                            crit, chain, two
#                                                            ladder tests, streak,
#                                                            monster down, heal cap,
#                                                            per-monster damage)
#         The set runs, ends and reviews                 7  (set completes, review is
#                                                            exactly the wrong items,
#                                                            empty review, feed level,
#                                                            defeat, session retired,
#                                                            locked node refused)
#         The island is the catalogue + the store        3
#         The store enforces the contract's laws         4  (mastery derived, scaffold
#                                                            one-way, streak per
#                                                            session, idempotent end)
#         The keypad produces the web's string          17  (5 digits + 5 decimals
#                                                            parameterised, fractions,
#                                                            refusals, cap, undo,
#                                                            submission, enablement)
#         Unit chips graded by the real engine           5
#         The chip row is deterministic                 20  (16 units parameterised +
#                                                            ordering, area example,
#                                                            equivalents, every unit
#                                                            teaches)
#         Fractions and decimals via the keypad          3
#         Figures: all seven, from drawn questions      17  (7 fit + 7 ink + lshape
#                                                            sides + unsupported +
#                                                            (the two suites' shared
#                                                            engine draw))
#         Every Quest screen fits both devices           4  (battle choice/typed,
#                                                            feedback, entrance +
#                                                            result, tap floor)
#         The driver and its transcript                  5
#         The keypad policy is measured                  2
#         The copy obeys the fade-out law                5
#         The buttons are on the glass, in pixels        3  (review list fits its
#                                                            plank, buttons on
#                                                            glass, title on glass)
#         (result + battle at real engine content)       2
#       Counted by CASE, which is what the runner's summary counts; 72 is the delta
#       the runner reported (71 -> 143).
#
# 160 = those 143 + MQQuest's 17, added on lane/quest in the fix pass, 2026-09-07,
#       against Quest Refutation - 2026-09-07:
#         The typed battle fits every matrix size        3  (8 topics x 12 sizes x 20
#                                                            REAL engine draws, all
#                                                            counted as one case per
#                                                            @Test; keypad keys drawn
#                                                            whole and on the glass at
#                                                            every size; every typed
#                                                            topic is in the gate)  K1
#         cm3/ml are never each other's distractors      2  (p5volume, p5decimals,
#                                                            through the real bridge) K3/K7
#         alias spellings are never distractors          1                            K3
#         the chip bridge, per topic                     4  (p4area, p5decimals,
#                                                            p5rate, p5volume, p3money
#                                                            - was one hard-coded
#                                                            p4area)                  K2
#         64 pause presses leave zero sessions open      1                            K6
#         two opens in one second are two sessions       1                            K6
#         nine wrong items across the review's pages     1  (3 sizes, one case)        K4
#         nine wrong items RENDERED across the pages     1  (9.7" portrait + SE)       K4
#         figure labels inside the box, floor binds      2  (p4data + p4pie, pixels)   K5
#         the driver drives the drawn views              2  (a session per matrix size
#                                                            + the --model path)   K1/driver
#         the keypad cap clears the engine's answerText  1                     wound 3
#         a half-written fraction is not submittable     1                     wound "3/"
#         the choice battle fits every matrix size    1  (4 figure topics x 12 sizes
#                                                          x 20 real draws; found and
#                                                          closed five pre-existing
#                                                          overflows nothing measured)
#       Counted by CASE, which is what the runner's summary counts; 17 is the delta
#       the runner reported (143 -> 160).
# 136 = those 71 + MQProgress's 65, added on lane/progress, 2026-09-07:
#         The scaffold never rises              6  (10,000 seeded random events; an
#                                                   up-request is a no-op; a failing run,
#                                                   a new session and the parent-screen
#                                                   reset all keep the fade)
#         The mastery climb matches the web     9  (a 509-history corpus recorded from
#                                                   js/app.js by tools/fixtures/
#                                                   gen-pool-climb.mjs, named edges, and
#                                                   the two counters that must NOT reset
#                                                   at the pool boundaries)
#         Node state derives from the record    9  (cleared / in progress / open / coming
#                                                   soon, incl. the zero-skill trap that
#                                                   would paint an unwritten island done)
#         Sessions summarise what happened     14  (in-session streak only, idempotent
#                                                   end, the crystal rule, the unit-vs-
#                                                   value split, the review list, caps)
#         JSON on disk, torn write loses none   8  (round trip, schema stamp, TWO injected
#                                                   write faults, temp litter, empty file,
#                                                   garbage, shared codec)
#         Schema migration                      9  (v0 missing, v0 unstamped, a hand-
#                                                   written v1 file, unknown scaffold
#                                                   name, a refused future schema)
#         Profiles are local and separate      10  (one child's fade never reaches
#                                                   another's; unique names; removal)
#       Measured per suite with `./test.command --filter MQProgressTests.<Suite>`; the
#       runner's summary counts test FUNCTIONS, so a parameterised @Test counts once
#       here even though it prints a line per case.
#
# 167 = those 136, plus 31 more from the FIX PASS on lane/progress, 2026-09-07 (the
#       twelve wounds of the Progress Refutation). Net of the tests that were rewritten
#       rather than added; the four suites that grew:
#         Crystals and Cleared, 200 real web sessions   14  (a new suite: the corpus's
#                                                            own shape; 200 sessions
#                                                            replayed against the web's
#                                                            crystal count; a mode that
#                                                            cannot inflate it; and the
#                                                            THREE NodeClearedPolicy
#                                                            implementations counted at
#                                                            2/3/4/7 skills)
#         Review rows keep every figure                 12  (a new suite: 216 rows through
#                                                            a real file, 0 lost, was 180;
#                                                            all 8 engine kinds plus one
#                                                            invented; the projection)
#         Privacy on disk, and how often it is touched  12  (a new suite: backup exclusion
#                                                            and its survival across the
#                                                            rename(2); the unstamped
#                                                            document; the load clamp;
#                                                            coalesced writes, flush(),
#                                                            6 structural write-throughs
#                                                            and atomicity under them)
#         The mastery climb matches the web             +8  (the load-time ceiling, 6
#                                                            parameterised histories where
#                                                            it must not out-fade the
#                                                            ladder, the ladder itself and
#                                                            its parameter)
#         Sessions / Profiles / Schema migration        +7  (the Patchwerk fence driven
#                                                            through record(); the split
#                                                            reason; a REAL rename and its
#                                                            persistence; v0-unstamped and
#                                                            v2 review columns)
#       Measured on Kai at the fix-pass commit: `./test.command` reported 167 executed.
